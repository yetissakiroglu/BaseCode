namespace Economy.Application.Interfaces.AppUserServices
{
    //public interface IAppUserServices
    //{
    //    Task<ResponseModel<Token>> CreateTokenAsync(SignIn signIn);               // Kullanıcı girişinde token üretimi
    //    Task<ResponseModel<Token>> RefreshTokenAsync(string refreshToken);       // Refresh token ile yeniden token üretimi
    //    Task<ResponseModel<string>> RevokeRefreshTokenAsync(string refreshToken); // Refresh token iptali
    //    ResponseModel<ClientToken> CreateToken(ClientSignIn clientSignIn);       // Client bazlı token üretimi (uygulama, sistem vs)

    //    Task<ResponseModel<bool>> LoginAsync(SignIn signIn);                     // Giriş doğrulama (sadece kontrol amaçlı)
    //    Task<ResponseModel<bool>> RegisterAsync(Register model);           // Kullanıcı kaydı
    //    Task<ResponseModel<AppUserDto>> GetCurrentUserAsync(string username);   // Kullanıcının bilgilerini getir
    //    Task<ResponseModel<bool>> ChangePasswordAsync(ChangePassword model); // Şifre güncelleme


    //    ////-Bakılacak
    //    //Task<AppUser?> GetUndeletedUserAsync(string email);
    //    //Task DeleteAsync(string? id);
    //}
}
