namespace Economy.Core.Enums
{
    public enum AppAccessMode
    {
        Blocked = 0,   // Tamamen kapalı
        LocalOnly = 1, // Sadece local geliştirme/localhost
        Public = 2     // Yayında herkese açık
    }
}
