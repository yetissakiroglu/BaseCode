@using Economy.Application.TenantUI.Dtos
@using Economy.Core.Enums

@model PageBlockIndexVm
@{
    ViewBag.Title = "Bloklar";
}

@* @{
    var unassigned = (IEnumerable<Block>)ViewBag.Unassigned;
}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.2/Sortable.min.js"></script>

<div class="row g-3">
  <div class="col-md-3">
    <div class="card h-100">
      <div class="card-header">Atanmayan Bloklar</div>
      <div class="card-body">
        <ul id="pool" class="list-group min-vh-50">
          @foreach (var b in unassigned)
          {
            <li class="list-group-item d-flex justify-content-between align-items-center" data-block-id="@b.Id">
              <span>@b.Title</span>
              <small class="text-muted">@b.Type</small>
            </li>
          }
        </ul>
      </div>
    </div>
  </div>

  @foreach (var g in Model.Items)
  {
    <div class="col-md-3">
      <div class="card h-100">
        <div class="card-header d-flex justify-content-between">
          <strong>@g.Tag</strong>
          <span class="text-muted">(@g.Blocks.Count)</span>
        </div>
        <div class="card-body">
          <ul class="list-group droplist" data-group-id="@g.Id">
            @foreach (var link in g.Blocks.OrderBy(x => x.SortOrder))
            {
              <li class="list-group-item d-flex justify-content-between align-items-center" data-block-id="@link.BlockId">
                <span>@link.Block.Title</span>
                <small class="text-muted">@link.Block.Type</small>
              </li>
            }
          </ul>
        </div>
      </div>
    </div>
  }
</div>

<div class="mt-3 d-flex justify-content-end">
  <button class="btn btn-outline-secondary me-2" onclick="location.reload()">Yenile</button>
  <button class="btn btn-primary" id="btnSave">Kaydet</button>
</div>

 *@





<main>
    <div class="container-fluid">
        @await Html.PartialAsync("_breadcrumbs")

        <h3>Page Blocks</h3>

        <form method="get" class="row g-2 mb-3">
            <div class="col-auto"><input class="form-control" type="number" name="pageId" value="@Model.FilterPageId" placeholder="PageId" /></div>
            <div class="col-auto">
                <select class="form-select" name="type">
                    <option value="">Type</option>
                    @foreach (var t in Enum.GetValues(typeof(BlockType)))
                    {
                        var v = (int)t;
                        <option value="@v" selected="@(Model.FilterType.HasValue && (int)Model.FilterType.Value == v)">@t</option>
                    }
                </select>
            </div>
            <div class="col-auto"><input class="form-control" name="q" value="@Model.Q" placeholder="Search slug/json" /></div>
            <div class="col-auto"><button class="btn btn-primary">Filter</button></div>
        </form>

        <div class="row row-cols-1 row-cols-md-2 row-cols-xl-3 g-3">
            @foreach (var it in Model.Items)
            {
                <div class="col">
                    <div class="card h-100">
                        <div class="card-header d-flex justify-content-between">
                            <div><strong>@it.Type</strong> <span class="text-muted">#@it.Id</span></div>
                        </div>
                        <div class="card-body">
                            <div><small>Tag:</small> @it.Tag </div>
                            <div><small>Page:</small> @it.BlockGroupId </div>
                            <div><small>Order:</small> @it.SortOrder</div>
                            <div><small>Active:</small> @(it.IsActive ? "Yes" : "No")</div>
                            <div><small>Stage:</small> @it.Stage</div>
                        </div>
                        <div class="card-footer d-flex gap-2">
                            <a class="btn btn-sm btn-primary" href="@Url.Action("Edit", "BlockGroupBlocks", new { area = "Tenant", id = it.Id })">Edit</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</main>

@section Scripts {
 @*    <script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.2/Sortable.min.js"></script>

    <script>
        (function(){
          // Havuz ve tüm grup listeleri için Sortable aktif et
          new Sortable(document.getElementById('pool'), { group: 'blocks', animation: 150 });
          document.querySelectorAll('.droplist').forEach(ul=>{
            new Sortable(ul, { group: 'blocks', animation: 150 });
          });

          document.getElementById('btnSave').addEventListener('click', async function(){
            // payload: her grup için sırayla blockId listesi
            const groups = [...document.querySelectorAll('.droplist')].map(ul => ({
              blockGroupId: parseInt(ul.dataset.groupId, 10),
              blockIds: [...ul.querySelectorAll('li[data-block-id]')].map(li => parseInt(li.dataset.blockId, 10))
            }));

            const resp = await fetch('/tenant/blocks/assign/save', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json', 'RequestVerificationToken': getCsrf() },
              body: JSON.stringify({ groups })
            });

            if (resp.ok) {
              alert('Kaydetme başarılı');
              // istersen location.reload();
            } else {
              alert('Kaydetmede hata');
            }
          });

          function getCsrf(){
            // sayfanda @Html.AntiForgeryToken() varsa hidden input’tan al:
            const el = document.querySelector('input[name="__RequestVerificationToken"]');
            return el ? el.value : '';
          }
        })();
    </script> *@
}