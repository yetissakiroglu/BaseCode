@page "/Account/login"
@layout Economy.AdminUI.Components.Layout.LoginLayout
@using Economy.Core.Dtos
@using Economy.Domain.Entites.Identities
@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Mvc
@inject SignInManager<AppUser> SignInManager
@inject NavigationManager Navigation



<PageTitle>Index</PageTitle>
<form class="app-form rounded-control needs-validation" asp-action="HandleLogin" method="post" novalidate>
    <div class="row">
        <div class="col-12">
            <div class="mb-5 text-center text-lg-start">
                <h2 class="text-primary-dark f-w-600">Welcome To axelit! </h2>
                <p>Sign in with your data that you enterd during your registration</p>
            </div>
        </div>
        <div class="col-12">
            <div class="mb-3">
                <label class="form-label" for="Email">Email</label>
                <input class="form-control" asp-for="Email" placeholder="Enter Your Email"
                       type="text" required>
            </div>
        </div>
        <div class="col-12">
            <div class="mb-3">
                <label class="form-label" for="password">Password</label>
                <a class="link-primary-dark float-end" href="password_reset.html">
                    Forgot Password
                    ?
                </a>
                <input class="form-control" asp-for="Password" placeholder="Enter Your Password"
                       type="password" required>
            </div>
        </div>
        <div class="col-12">
            <div class="form-check mb-3">
                <input class="form-check-input" id="checkDefault" type="checkbox" value="">
                <label class="form-check-label text-secondary" for="checkDefault">
                    Remember me
                </label>
            </div>
        </div>
        <div class="col-12">
            <div class="mb-3">
                <button type="submit" class="btn btn-light-primary w-100">
                    Giriş Yap
                </button>
            </div>
        </div>


    </div>
</form>
@code {
    private SignIn signInModel = new SignIn();
    private string loginErrorMessage;

    [HttpPost]
    private async Task HandleLogin()
    {
        var result = await SignInManager.PasswordSignInAsync(signInModel.Email, signInModel.Password, signInModel.RememberMe, false);

        if (result.Succeeded)
        {
            // Giriş başarılı ise, anasayfaya yönlendir
            Navigation.NavigateTo("/");
        }
        else
        {
            if (result.IsLockedOut)
            {
                loginErrorMessage = "Hesabınız kilitlenmiş.";
            }
            else if (result.RequiresTwoFactor)
            {
                loginErrorMessage = "İki faktörlü kimlik doğrulama gerekiyor.";
            }
            else
            {
                loginErrorMessage = "Geçersiz giriş denemesi.";
            }
        }
    }
}

@* <!-- Body main section starts -->
<div class="container">
    <div class="row sign-in-content-bg">
        <div class="col-lg-6 image-contentbox d-none d-lg-block">
            <div class="form-container ">
                <div class="signup-content mt-4">
                    <span>
                        <img alt="" class="img-fluid " src="../assets/images/logo/1.png">
                    </span>
                </div>

                <div class="signup-bg-img">
                    <img alt="" class="img-fluid" src="../assets/images/login/01.png">
                </div>
            </div>

        </div>
        <div class="col-lg-6 form-contentbox">
            <div class="form-container">


                <EditForm Model="@signInModel" OnValidSubmit="HandleLogin">
                    <DataAnnotationsValidator />
                    <ValidationSummary />

                    <div class="row">
                        <div class="col-12">
                            <label for="email">Email</label>
                            <InputText id="email" class="form-control" @bind-Value="signInModel.Email" placeholder="Enter Your Email" type="email" />
                            <ValidationMessage For="@(() => signInModel.Email)" />
                        </div>
                        <div class="col-12">
                            <label for="password">Password</label>
                            <InputText id="password" class="form-control" @bind-Value="signInModel.Password" placeholder="Enter Your Password" type="password" />
                            <ValidationMessage For="@(() => signInModel.Password)" />
                        </div>
                        <div class="col-12">
                            <button type="submit" class="btn btn-light-primary w-100">Login</button>
                        </div>
                    </div>
                </EditForm>
                
                @if (!string.IsNullOrEmpty(loginErrorMessage))
                {
                    <div class="alert alert-danger mt-3">
                        @loginErrorMessage
                    </div>
                }

                
            </div>
        </div>
    </div>
</div>
<!-- Body main section ends -->
<!--js-->
*@